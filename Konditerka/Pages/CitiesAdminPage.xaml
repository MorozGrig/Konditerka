<Page x:Class="Konditerka.Pages.CitiesAdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="760"
      Title="CitiesAdminPage">
    <Grid Background="#FF420046">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DataGrid x:Name="CitiesGrid" AutoGenerateColumns="False" SelectionChanged="CitiesGrid_SelectionChanged" FontFamily="Arial Black" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdCity}" Width="80"/>
                <DataGridTextColumn Header="Название города" Binding="{Binding NameCity}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,0">
                <Label Content="Название города" Foreground="#FF82DBFF" FontFamily="Arial Black"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox x:Name="CityNameBox" Width="300" Height="30" Margin="0,0,10,0" FontFamily="Arial Black" TabIndex="50" MaxLength="50" ToolTip="Название города" Background="#FF91009A" BorderBrush="#FFE0C14D" Foreground="#FF82DBFF">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <Button Content="Добавить" Width="120" Height="30" Margin="0,0,8,0" Click="AddButton_Click" FontFamily="Arial Black" Foreground="Black" Background="#FFE0C14D"/>
                <Button Content="Сохранить" Width="120" Height="30" Margin="0,0,8,0" Click="EditButton_Click" FontFamily="Arial Black" Foreground="Black" Background="#FFE0C14D"/>
                <Button Content="Удалить" Width="120" Height="30" Margin="0,0,8,0" Click="DeleteButton_Click" FontFamily="Arial Black" Foreground="Black" Background="#FFE0C14D"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
