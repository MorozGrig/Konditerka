<Page x:Class="Konditerka.Pages.OrdersAdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="760"
      Title="OrdersAdminPage">
    <Grid Background="#FF420046">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DataGrid x:Name="OrdersGrid" AutoGenerateColumns="False" SelectionChanged="OrdersGrid_SelectionChanged" FontFamily="Arial Black" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding IdOrder}" Width="60"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding IdUser}" Width="100"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Data}" Width="150"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusOrders.NameStatusOrder}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox  x:Name="UserIdBox" Width="90" Height="30" Margin="0,0,8,0" FontFamily="Arial Black" ToolTip="ID клиента" TabIndex="50"/>
            <DatePicker x:Name="OrderDateBox" Width="140" Height="30" Margin="0,0,8,0" FontFamily="Arial Black" DisplayDateStart="2026/03/30"/>
            <TextBox x:Name="OrderPriceBox" Width="120" Height="30" Margin="0,0,8,0" FontFamily="Arial Black" ToolTip="Сумма" TabIndex="50"/>
            <ComboBox x:Name="StatusBox" Width="180" Height="30" Margin="0,0,8,0" DisplayMemberPath="NameStatusOrder" SelectedValuePath="IdStatusOrder" FontFamily="Arial Black"/>
            <Button Content="Добавить" Width="100" Height="30" Margin="0,0,8,0" Click="AddButton_Click" FontFamily="Arial Black" Foreground="Black" Background="#FFE0C14D"/>
            <Button Content="Сохранить" Width="100" Height="30" Margin="0,0,8,0" Click="EditButton_Click" FontFamily="Arial Black" Foreground="Black" Background="#FFE0C14D"/>
            <Button Content="Удалить" Width="100" Height="30" Margin="0,0,8,0" Click="DeleteButton_Click" FontFamily="Arial Black" Foreground="Black" Background="#FFE0C14D"/>
        </StackPanel>
    </Grid>
</Page>
